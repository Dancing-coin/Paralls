# 07-406-430承载能力深度审计

## 说明

本审计用于回答：`406-430` 第一批新增内容是否已被现有 core 文档完整承载。

审计规则：

- 主源仅使用 `filtered_conversation_split`
- 目标轮次按相邻规则核对：本次核 `406-430`，并核对 `401-405` 与 `431-437`
- 只判定“可执行承载能力”，不判定“是否出现关键词”

## 审计对象

- `D104`~`D108`（建造线、世界引擎形态、权限分层、映射更新、生物/物件创作边界）

## 审计结论（总览）

| 决策ID | 覆盖判断 | 结论 |
| :--- | :---: | :--- |
| `D104` | 部分承载 | 方向已冻结，缺执行协议 |
| `D105` | 部分承载 | 口径已冻结，缺适配契约 |
| `D106` | 部分承载 | 原则已冻结，缺权限矩阵 |
| `D107` | 部分承载 | 边界已冻结，缺映射与冲突规则 |
| `D108` | 部分承载 | 归属已冻结，缺创作链路设计 |

总判断：现有 core 对 `D104-D108` 为“全量部分承载”，不构成“完整承载”。

## 逐项证据

### D104 建筑蓝图闭环优先

- 目标轮次要点：`406-407` 从“我的世界式”收束到“建筑蓝图建造/交易/导入生成”。
- 已覆盖：
  - `技术架构总纲` 已写“建筑蓝图闭环优先”与阶段边界
  - `核心玩法机制设计` 已写玩法收束
  - `Phase 1 项目计划书` 已写交付收束
- 缺口：
  - 蓝图对象模型（结构、版本、依赖）
  - 导入生成校验流程（权限、资源、冲突）
  - 运行时回写语义（实例与模板关系）

### D105 世界引擎形态是 Godot + 规则封装

- 目标轮次要点：`410-414` 收束“非重写引擎”。
- 已覆盖：
  - `技术架构总纲`、`Phase 1 项目计划书` 已写口径
- 缺口：
  - 规则封装层接口（输入事件、状态回写、异常处理）
  - 引擎升级兼容策略（运行时版本约束）
  - 与 ESM / 事件总线的边界清单

### D106 创作者层与玩家层权限隔离

- 目标轮次要点：`418` 明确“创作者可捏，玩家遵循玩法边界”。
- 已覆盖：
  - `技术架构总纲`、`核心玩法机制设计`、`商业与生态设计` 写了分层原则
- 缺口：
  - 操作级权限矩阵（创建、修改、发布、下架、回滚）
  - 权限审计与违规回收路径
  - 大玩家与创作者权限交叉规则

### D107 场景级与世界级分层 + 映射更新

- 目标轮次要点：`408` 提出场景/世界分层并要求地图映射更新；`416/419` 继续强化。
- 已覆盖：
  - `技术架构总纲` 已写“场景级/世界级边界”
- 缺口：
  - 映射同步协议（何时同步、同步粒度、方向）
  - 冲突合并策略（同一资产多方修改）
  - 回写边界（允许回写什么，不允许回写什么）

### D108 生物/物件可捏制归创作者工具层

- 目标轮次要点：`409` 提出“生长/繁殖/组建/安装”的设计需求，`417-418` 收束权限归属。
- 已覆盖：
  - `核心玩法机制设计` 写了归属原则
  - `ESM设计文档` 写了环境参数与后续生长能力后移
- 缺口：
  - “形态编辑-参数配置-行为执行”三层链路
  - 与 `ESM` 生长规则及资源规则的耦合方式
  - Phase 1/2/3 的分步交付切片

## 第二轮回填状态（本轮更新）

- 上述“缺口”已在 `世界构建与创作系统设计.md` 完成文档级补齐：
  - 导入请求/响应协议
  - 校验错误码
  - 映射同步事件协议
  - 冲突审核工单协议
  - 审计字段与 Phase 1 量化阈值
- 当前剩余风险从“文档缺失”转为“实现联调与阈值校准”。

## 体系建议

已新增（初稿）：

- `docs/core/世界构建与创作系统设计.md`

该文档职责：

- 统一承接 `D104-D108`
- 提供可执行接口与权限/映射规则
- 作为 `技术架构总纲`、`核心玩法机制设计`、`商业与生态设计`、`Phase 1 项目计划书` 的上游约束文档

## 与第二批新增的关系

`431-437` 主要是硬件与云地策略修订，属于部署与成本边界，不改变本审计结论。

- 第二批不会替代第一批在“世界构建与创作系统”上的结构性缺口
- 因此“新增 1 个 core”仍然成立
